Prebuilt versions of Bullet, Assimp and Boost are expected to be installed on the system.
The build system uses CMake's `find_package` to locate them.
See `setup.sh` for the list of required packages.
